# Official DIA oracle Total Value Secured (TVS), from DefiLlama.
#
# WHY THIS EXISTS: the tool's "DIA-linked TVL proxy" (config/protocols.yaml) sums
# the WHOLE TVL of protocols that use DIA somewhere — it is an upper bound on
# *reach*, not the value DIA actually secures, and it overstates real TVS by
# ~16-76x. DefiLlama computes the real per-oracle TVS (only the markets/pools a
# given oracle actually secures). The tool now pulls this live from DefiLlama's
# free /oracles endpoint (or Pro /api/oracles if DEFILLAMA_API_KEY is set); this
# manual figure is the fallback used only when neither is reachable.
#
# VERIFY FREE (no account) at: https://defillama.com/oracles/DIA
# Refresh periodically; DefiLlama Pro would automate this + add history.

defillama_tvs_usd: 148230000
source: "https://defillama.com/oracles/DIA"
as_of: "2026-06-13"
top_protocols: "Zest (~48%), Rezerve Lending (Ethereum), FortiFi (Avalanche)"
note: "Real value DIA's oracles secure. Compare to mcap for an adoption-vs-price read."
